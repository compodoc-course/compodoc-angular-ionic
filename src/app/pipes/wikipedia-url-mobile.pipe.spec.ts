import { WikipediaUrlMobilePipe } from './wikipedia-url-mobile.pipe';

describe('WikipediaUrlMobilePipe', () => {
  it('create an instance', () => {
    const pipe = new WikipediaUrlMobilePipe();
    expect(pipe).toBeTruthy();
  });
});
